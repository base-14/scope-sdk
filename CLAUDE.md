# CLAUDE.md - Scope SDK Repository Guidelines

## File Access Rules

- **Never** read, search, or glob inside `.venv/`, `node_modules/`, or other dependency/vendor directories unless the user explicitly asks to.
